name: stayAwake-workFlow.yml
on:
    schedule:
      - cron: "*/5 * * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://your-render-app.onrender.com


